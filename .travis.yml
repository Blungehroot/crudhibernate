language: java

before_script:
  - psql -c 'create database crudhibernate' -U postgres

cache:
  directories:
    - $HOME/.m2

script:
  - java --version
  - ./mvnw clean install
  - ./mvnw test

services:
  - postgresql